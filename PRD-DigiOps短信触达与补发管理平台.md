# PRD：DigiOps 短信触达与补发管理平台（V1 定版）

> 版本：V1（定版） · 日期：2026-08-17 · 状态：原型已完成，需求冻结
> 本文档基于已定版的高保真原型与主规格整理，是后续开发、测试与验收的需求依据。

## 1. Summary

本文档定义 DigiOps 短信触达与补发管理平台第一版需求。平台让运营人员能够完整追踪短信发送与送达状态，对失败、未送达、回执超时的短信进行受控补发，统一管理黑名单，并在运营计划中配置“发送前校验 + 失败后补发”的策略。当前高保真原型已定版，本文档与原型互为依据。

## 2. Contacts

| 姓名 / 角色 | 职责 | 备注 |
| --- | --- | --- |
| 产品经理 | 需求定义、原型验收、版本决策 | 本文档负责人 |
| 研发 | 接口联调、状态机实现、自动补发扫描任务 | 以本文档 + 原型为准 |
| 测试 | 用例设计、验收 | 以本文档验收标准为准 |
| 运营 | 使用方代表，确认业务口径 | 提供真实运营场景反馈 |

## 3. Background

DigiOps 平台已具备短信发送能力，但存在三个业务痛点：

1. **发送后不可追踪**：短信发出后缺少回执状态沉淀，无法区分“已送达、未送达、回执超时”，运营只能凭经验判断效果。
2. **失败短信无人补救**：发送失败或未送达的短信没有自动重试机制，触达效果和用户覆盖受损。
3. **打扰不可控**：黑名单分散管理，计划执行前没有统一的前置拦截，容易重复打扰用户。

为什么是现在：短信回执信息落库能力已经排期（回执以 messageId 关联写入发送记录），送达状态、补发判断、报表统计有了数据基础。本版本先以高保真原型冻结产品方案，为后续开发提供唯一需求依据。

## 4. Objective

目标：让运营人员可以追踪每条短信的发送结果，对失败短信做有规则、有记录、可干预的补发，并通过黑名单与发送时段减少对用户的无效打扰。

关键结果（KR）：

- 发送记录页完整展示发送状态与送达状态，覆盖成功、失败、暂无数据、回执中、已送达、未送达、回执超时七种场景。
- 人工补发全流程可走通：条件筛选 → 命中汇总 → 校验 → 确认补发 → 批次跟踪，批次状态覆盖六态（待执行 / 执行中 / 已完成 / 已终止 / 失败 / 部分失败）。
- 自动补发规则可配置、可开关，开启后系统按规则自动聚批补发；黑名单用户默认不参与自动补发。
- 黑名单支持新增、导入、导出、生效、失效、移除与批量管理，作为人工补发、自动补发和运营计划共用的拦截资产。
- 运营计划画布支持“前置校验 + 补发控制”配置，实现计划级“谁能发、什么时候发、失败了怎么补”。

## 5. Market Segment

使用方不是外部市场客户，而是 DigiOps 平台的内部角色：

- **运营人员**：日常查看发送记录与报表，发起人工补发，维护黑名单。
- **风控 / 合规人员**：依赖黑名单拦截与发送时段控制，确保不重复打扰用户。
- **产品 / 运营策略人员**：在运营计划画布中配置前置校验与补发控制策略。

约束：

- 当前原型不包含真实登录鉴权与权限控制。
- 数据使用 UAT 快照 / mock 数据，查询、校验、导出为原型级交互。

## 6. Value Proposition

| 用户要完成的任务 | 本产品提供的价值 |
| --- | --- |
| 判断一条短信到底发没发到 | 发送状态 + 送达状态双状态展示，回执超时 24 小时明确兜底 |
| 失败 / 未送达的短信怎么办 | 人工补发按条件圈选；自动补发按规则聚批、定时补偿 |
| 避免重复打扰黑名单用户 | 黑名单全局生效：人工补发可选手动过滤，自动补发默认强制排除 |
| 控制发送时间 | 生效时段 / 允许发送时段配置，非允许时段自动挂起 |
| 复盘触达效果 | 补发统计、报表、批次详情与发送记录联动 |
| 计划级策略管控 | 画布中配置前置校验与补发控制，全局规则兜底 |

相比“发出即结束”的现状，本方案把短信触达从单次动作变成可追踪、可补偿、可治理的闭环。

## 7. Solution

### 7.1 UX / Prototypes

- 高保真原型（可交互演示）：本地 Make 地址 `http://localhost:53817/?projectId=make-project-20260730&p=sms-message`
- 演示站（GitHub Pages）：随每次提交自动更新
- 主规格文档：`src/prototypes/sms-message/.spec/spec.html`

### 7.2 Key Features

#### 模块一：短信管理

**发送记录**

- 搜索区 11 个筛选字段：发送时间、BusinessID、计划名称、用户分组、手机号码、内容类型、发送名称、发送状态、送达状态、补发批次 ID、路径标记。
- 列表 13 列，支持横向滚动、分页（10 条 / 页）、导出（导出名称必填）。
- 送达状态机（用户已确认）：
  - 发送状态成功 → 回执中 / 已送达 / 未送达 / 回执超时（24 小时内无明确回执）；
  - 发送状态失败 → `--`；发送状态暂无数据 → `--`；历史短信 → `--`。
- 状态列 hover 气泡说明，补发批次 ID 可点击跳转补发记录。

**模板 / 报表**

- 模板：搜索、新建入口、模板列表（本期模板数据为空）。
- 报表：近 7 天 / 近 30 天 / 近半年切换、发送量折线图、到达率环形图、费用统计、发送明细表（演示数据）。

#### 模块二：补发中心

**人工补发**

- 条件筛选（与发送记录同款字段）：默认不选任何条件时查询按钮置灰，hover 提示“请至少选择一项筛选条件”。
- 命中汇总：展示“当前筛选项下命中 X 条，其中黑名单命中 Y 条，任务执行时将重新校验”；命中 0 条时红字提示。
- 补发弹窗（单弹窗完成）：补发方式、预计补发时间（定时可编辑且须晚于当前时间）、黑名单用户是否发送（默认不选中）、黑名单提示、补发数量（点击校验后展示）；校验完成前确认键置灰，修改条件后校验结果自动重置。
- 补发记录（批次维度）：补发批次 ID（B 前缀）、补发开始 / 结束时间、补发方式、提交校验数量、实际发送数量、补发状态、操作（详情 / 终止，终止需二次确认）。
- 批次状态机六态：待执行、执行中、已完成、已终止、失败、部分失败；“已完成”定义为“对每条短信都做了发送动作”，明细发送状态不影响批次状态。

**自动补发**

- 顶部状态条：自动补发开关独立展示，开启后显示队列摘要（队列中 X 条 · 预计发送时间 · 满 X 条提前触发）。
- 规则配置（必填项带红星，默认关闭可编辑、开启后锁定）：
  - 触发条件：提交失败时（发送失败）；回执判定后（未送达 / 回执超时）；
  - 最多补发次数（=1 时补发时间间隔置灰且不参与必填）；
  - 补发时间间隔（同一条短信两次自动补发之间的最小间隔）；
  - 最大排队数量（固定档位 100 / 500 / 1,000 / 2,000 / 5,000 / 10,000，默认 1,000）；
  - 批次最长等待（从批次首条短信入队起算）；
  - 生效时段（默认不替用户选，须显式选择全天或指定时段，时段不得重合）。
- 补发统计：累计补发 / 补发成功 / 补发失败 / 待确认 / 队列中，支持按运营计划筛选；补发成功 = 发送成功且已送达，补发失败 = 发送失败或未送达 / 回执超时，待确认 = 其余情况。
- 自动补发记录（批次维度）：补发批次 ID（A 前缀）、当前状态、开始 / 结束时间、实际发送数量、详情；不提供单批次终止（止血用全局开关）。
- 业务规则：2026.8.24 日前的历史短信不参与自动补发；全局规则默认生效，运营计划配置了自动补发组件时以计划配置为准；自动补发默认强制校验黑名单。

#### 模块三：黑名单（用户管理子边栏）

- 统计卡：名单总数 / 生效中 / 已失效 / 今日拦截。
- 筛选：手机号码、BusinessID、状态、添加日期（起止）。
- 新增黑名单：手机号码批量输入（每行一个）、BusinessID、有效期（永久 / 指定时间，默认不选中；“指定时间”为开始日期 → 结束日期，均由用户填写）。
- 列表操作：查看详情、生效、失效、移除（均二次确认）；复选框批量操作（批量生效 / 失效 / 移除 / 导出选中）；导出弹窗（导出名称必填，模拟成功与失败）。
- 语义：失效 = 暂停拦截、名单保留；生效 = 恢复参与人工补发、自动补发及运营计划拦截校验；移除 = 永久删除。

#### 模块四：运营计划管理

- 计划列表：批次信息 + 操作列（详情进入画布），支持新建计划入口。
- 流程画布：节点面板（进入 / 动作 / 渠道）、拖拽入画布、节点移动、四向端口连线、节点配置面板、本地保存（刷新后可继续编辑）。
- 判断节点事件类型新增“前置校验”：校验项（黑名单校验、发送时段校验）、允许发送时段（最多 3 段，重合拦截）、“查看黑名单”可跳转黑名单页。
- 补发控制（动作组独立组件）：触发条件（提交失败 / 未送达 / 回执超时）、最大补发次数、补发间隔；流程在补发完成（成功或达上限）后继续，补发前复用画布前置校验规则。
- 网关节点最多 2 条出线：第一条自动标记 YES（绿✓），第二条 NO（红✕），超限拦截。

#### 通用能力

- 列设置：补发记录、运营计划管理列表支持勾选列显隐（操作列固定）。
- 导出弹窗：导出名称必填、默认 Excel；部分场景支持失败模拟（失败时弹窗不关闭并提示）。
- 状态与新增功能视觉标记：本次新增 / 增强的 Tab 与控件使用淡紫色背景（呼吸灯动画）便于识别。

### 7.3 Technology

- 高保真原型：React + TypeScript + Vite，运行于 Axhub Make。
- 数据：mockData 本地快照；画布节点与连线使用 localStorage 持久化。
- 部署：GitHub Actions 自动构建短信原型单入口并发布 GitHub Pages，每次提交自动更新演示站。
- 本期不接真实接口：查询、校验、导出、状态推进均为前端模拟。

### 7.4 Assumptions

- 短信回执落库后，发送记录可拿到 messageId 与回执状态，支撑送达状态机。
- 自动补发扫描周期为后端参数（建议 5 分钟），不在规则页暴露。
- 批次执行采用“分批滚动校验 → 发送”模型：实际发送数量在执行过程中累计，提交校验数量只可能减少。
- 失败批次暂不提供“重试”操作，待后续版本确认。
- 报表图表与费用数据为演示占位，真实口径待报表需求单独确认。
- 黑名单“今日拦截”等统计依赖拦截日志，本期为 mock。

## 8. Release

**V1（本期，已定版）**

- 短信管理：发送记录（含送达状态机）、模板、报表。
- 补发中心：人工补发全流程、自动补发规则与记录、六态批次状态机、详情抽屉、列设置与导出。
- 黑名单：完整名单库管理（新增 / 导入 / 导出 / 生效 / 失效 / 移除 / 批量）。
- 运营计划：列表 + 画布（拖拽 / 连线 / 前置校验 / 补发控制 / 本地持久化）。
- 交付物：高保真原型 + 本文档 + 主规格；开发以原型和规格为准。

**V2（候选，未排期）**

- 模板新建 / 编辑完整表单与试发。
- 失败批次重试。
- 报表真实口径与数据接入。
- 真实接口联调（查询、校验、导出、回执状态推进）。
- 自动补发扫描周期可视化管理。

**时间预期**

- 原型与需求已冻结，可随时进入研发排期。
- 建议研发先实现“回执落库 → 发送记录状态展示 → 人工补发 → 自动补发扫描任务”的链路，再接入运营计划画布策略。
