# 项目上下文（给接手 AI 的快速说明）

> 其他 AI / 协作者请先读本文件 + `AGENTS.md`（工作规则）+ `src/prototypes/sms-message/.spec/spec.html`（主规格，事实真值）；产品需求见本地 `PRD-DigiOps短信触达与补发管理平台.md`（不同步 GitHub）。

## 这是什么

DigiOps 短信触达与补发管理平台的高保真可交互原型（Axhub Make 项目），用 React + TypeScript + Vite 实现。当前阶段：原型已定版，随产品讨论持续迭代，用于评审、演示和后续开发依据。

## 如何运行

- 项目根目录执行 `npm run dev`（启动 vite 51720 + Make 53817）
- 本地预览：`http://localhost:53817/?projectId=make-project-20260730&p=sms-message`
- 演示站（GitHub Pages）：`https://phosphorescent2018.github.io/0814-Digiops-message/`，每次 push main 自动部署
- 仓库：`phosphorescent2018/0814-Digiops-message`

## 当前范围（已实现）

| 模块 | 位置 | 说明 |
| --- | --- | --- |
| 发送记录 | `pages/RecordPage.tsx` | 14 列表格、12 字段搜索、送达状态机、导出；新增「补发类型」筛选与列（原始短信 / 人工补发 / 计划内自动补发·第 N 次） |
| 人工补发 | `pages/ResendCenter.tsx` | 原「补发中心」更名；移除人工补发/自动补发子 Tab 与全局自动补发页面；当前为人工补发（筛选→命中→单弹窗校验→批次六态） |
| 黑名单 | `pages/BlacklistPage.tsx` | 用户管理子边栏入口，名单库管理（新增/导入/导出/生效/失效/移除/批量） |
| 运营计划管理 | `pages/OperationPlanPage.tsx` / `OperationPlanCanvas.tsx` | 计划列表 + 画布（拖拽/连线/短信节点内置校验与补发控制/本地持久化） |

## 关键口径（最近确认）

- 送达状态：成功 → 回执中 / 已送达 / 回执超时；失败、暂无数据、历史短信 → `--`（2026-08-21 删除「未送达」）
- 补发类型枚举：原始短信 / 人工补发 / 计划内自动补发；发送记录表格与筛选均支持
- 人工补发：查询防呆（无筛选置灰）、筛选含补发批次 ID / 补发状态 / 路径标记、单弹窗完成配置+校验、黑名单选项默认不选、定时时间须晚于当前
- 批次状态机六态：待执行 / 执行中 / 已完成 / 已终止 / 失败 / 部分失败；「已完成」= 每条短信都做了发送动作
- 数量口径：提交校验数量（提交前用户校验）；实际发送数量（分批校验通过并执行发送动作的累计条数，含所有发送状态）；按原始短信维度查重为讨论中方向，未落地
- 自动补发：独立页面已移除；补发控制已并入短信节点（启用开关 / 触发条件 / 最大补发次数 / 补发间隔）

## 最近迭代（git log 可查）

- v1.0.23 人工补发记录分页改为发送记录同款（‹ › 翻页 / 页码 / 10·20·50 条每页 / 跳至，默认 10）
- v1.0.22 修复人工补发筛选重复「补发状态 / 路径标记」（网格与末行去重，选项恢复完整）
- v1.0.21 人工补发记录分页增加每页条数选择
- v1.0.20 人工补发详情删除导出按钮、记录列表新增右下角分页、补发类型/补发状态下拉默认「全部」改深色
- v1.0.19 补发中心更名「人工补发」，移除全局自动补发页面（AutoResend / AutoBatchDetail）
- v1.0.17 发送记录表格新增「补发类型」列
- v1.0.16 送达状态删除「未送达」（下拉/触发条件/统计/详情同步）
- v1.0.15 发送记录新增「补发类型」筛选、人工补发新增「补发状态」筛选
- v1.0.14 自动补发详情规则快照同步「队列最长等待」
- 更早：黑名单移入用户管理子边栏、人工补发弹窗合并、导出弹窗默认 Excel 等（见 spec.html 决策记录）

## 正在讨论（尚未落地，勿当作已定）

补发模型重构方向（部分已落地，以下仍待确认）：

1. 已落地：移除全局自动补发页面/Tab；自动补发仅存在于短信节点内置补发控制（逐条重试，无批次）；
2. 防重复采用「原始短信」口径：每条短信记录原始短信、补发类型；人工补发只筛原始短信；双方发出前查重（自动=跳过，人工=提示+默认剔除可强制）；自动补发按原始短信计次——仍待落地；
3. 短信节点补发控制补「允许补发时段」，黑名单默认强制——仍待落地；
4. 待拍板：全局规则是否保留为模板、组件启停开关、发送记录「补发类型」列是否展开查看、无计划短信无自动兜底是否接受。

## 协作规则（重要）

- 改动前先读相关代码，说明方案，确认后改，改后自测；
- 每次 git 提交必须同步主规格（`.spec/spec.html`），提交后打语义化 tag 并推送，随后监控 GitHub Actions 部署；
- PRD 文档本地维护，不同步 GitHub（已 gitignore `PRD-*.md`）。
